import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Finance',
};

export default function FinancePage() {
  return <section>{/* Finance module — Phase 5 */}</section>;
}
