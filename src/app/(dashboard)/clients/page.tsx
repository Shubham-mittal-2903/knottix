import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Clients',
};

export default function ClientsPage() {
  return <section>{/* Clients module — Phase 2 */}</section>;
}
