import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Creative',
};

export default function CreativePage() {
  return <section>{/* Creative module — Phase 2 */}</section>;
}
